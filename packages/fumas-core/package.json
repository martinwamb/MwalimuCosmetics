{
  "name": "@mwalimu/fumas-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared contract layer for the mwalimuinvest MySQL database: auth crypto, money maths, and ledger posting. Consumed by the desktop app and the sync agent.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "mysql": "2.18.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
