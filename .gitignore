node_modules
.next
dist
.turbo
.vercel
coverage
.env
.env.local
pnpm-lock.yaml
package-lock.json
yarn.lock
*.log

mwalimu/

# Claude Code local settings
.claude/

# Bridge build artifacts and sensitive scripts
bridge/MwalimuSyncAgent.zip
bridge/fix-mysql-privileges.bat

# Desktop build output
apps/desktop/dist-electron
apps/desktop/release
apps/desktop/*.log
bridge/schema-probe-output/
apps/front/tsconfig.tsbuildinfo

# Audit output — real customers, real sales, real money. Findings belong in
# commit messages; the rows themselves stay on the machine that produced them.
bridge/drafts-output/
bridge/orphans-output/
bridge/diagnose-output/
